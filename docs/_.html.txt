layout - контейнер страницы с шапкой, футером и тд
page-content - контейнер контента, содержимое без шапки и футера

svg-иконки вставлял либо через img, либо через html-тэг use
(иконки используемые на странице подгружал в начале body).
Это тоже можно переделать.